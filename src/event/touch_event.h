#ifndef _TOUCH_EVENT_H
#define _TOUCH_EVENT_H

#include "event/event_dispatcher.h"

class touch_event : public event {
    
};

class touch_began_event : public event {
    
};

class touch_moved_event : public event {
    
};

class touch_ended_event : public event {
    
};

class touch_cancelled_event : public event {
    
};

#endif